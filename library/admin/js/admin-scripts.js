// add admin scripts here
var $ = jQuery.noConflict();
$(document).ready(function() {

	// Use jquery validate for all our admin validation needs (https://jqueryvalidation.org/)
	// $( "#post" ).validate({
	//   errorLabelContainer: ".messageBox",
	//   rules: {
	//     wpbs_featured_desc: {
	//       required: false,
	//       maxlength: 200,
	//     }
	//   },
	//   highlight: function(element, errorClass) {
	//     $(element).parent().find('.messageBox').addClass('error');
	//     $(element).fadeOut(function() {
	//       $(element).fadeIn();
	//     });
	//   },
	//   unhighlight: function(element, errorClass, validClass) {
	//     $(element).parent().find('.messageBox').removeClass('error');
	//   }

	// });

});